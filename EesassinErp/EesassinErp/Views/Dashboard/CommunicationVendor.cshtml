 
@{
    ViewBag.Version = System.Configuration.ConfigurationManager.AppSettings["version"];
    ViewBag.Title = "Communication Vendor";
    Layout = "~/Views/Common/_NewRetailLayout.cshtml"; 
}
<script src="~/Scripts/myjslib/WithoutInvoiceController.js?Ver=@ViewBag.Version"></script>
<style>
    #communicationVendorPage {
        font-family: "Inter", sans-serif;
        --primary: #E45D27; /* theme accent */
        --primary-dark: #c44b1d;
        --card-bg: #FEF3EC;
        background: #FFF1EB;
        color: #223;
        min-height: 85vh;
    }

    .dropdown-menu {
        min-width: 120px;
        font-size: 0.85rem;
        border-radius: 6px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.12);
    }

    #communicationVendorPage #createComplDoc {
        background: var(--primary);
        color: #fff;
        border: none;
        border-radius: 999px;
        padding: 6px 12px;
        font-weight: 400;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        -webkit-tap-highlight-color: transparent;
    }

        #communicationVendorPage #createComplDoc:hover {
            background: var(--primary-dark);
        }


    /* Section cards */
    #communicationVendorPage .section-card {
        background: transparent;
        border: none;
    }

        #communicationVendorPage .section-card .card-body {
            background: var(--card-bg);
            border-radius: 12px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.06);
            min-height: 320px;
        }

    /* Table wrapper and hide scrollbar visually */
    #communicationVendorPage .small-table-wrapper .table-responsive {
        max-height: 75vh;
        overflow-y: auto;
        scrollbar-width: none; /* Firefox */
        -ms-overflow-style: none; /* IE and Edge */
        scroll-behavior: smooth;
        position: relative;
    }

        #communicationVendorPage .small-table-wrapper .table-responsive::-webkit-scrollbar {
            display: none;
        }

    /* table header sticky */
    #communicationVendorPage .small-table thead th {
        position: sticky;
        top: 0;
        z-index: 1;
        background-color: #F37437;
        color: #fff;
        font-weight: 600;
        font-size: 0.78rem;
        text-align: left;
        vertical-align: middle;
        white-space: nowrap;
        padding: 8px 10px;
    }

    /* table body */
    #communicationVendorPage .small-table tbody td {
        background-color: var(--card-bg);
        font-size: 0.78rem;
        vertical-align: middle;
        text-align: left;
        padding: 8px 10px;
        white-space: nowrap;
    }

    /* hover */
    #communicationVendorPage .small-table tbody tr:hover td {
        background-color: #FFE8D9;
    }

    #communicationVendorPage .small-table tbody td .store-code-link,
    #communicationVendorPage .small-table tbody td .event-link {
        text-decoration: none;
    }

    /* action menu */
    #communicationVendorPage .action-menu {
        position: relative;
        width: 48px;
        text-align: right;
        background-color: var(--card-bg);
    }

        #communicationVendorPage .action-menu .btn {
            color: var(--primary);
        }

        #communicationVendorPage .action-menu .dropdown-menu {
            min-width: 120px;
            font-size: 0.85rem;
            border-radius: 6px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.12);
        }

    /* filter button */
    #communicationVendorPage .filter-btn {
        border-radius: 999px;
        border: 1px solid var(--primary);
        color: var(--primary);
        background: transparent;
        padding: 6px 14px;
        font-weight: 400;
        font-size: 0.85rem;
    }

        #communicationVendorPage .filter-btn.active,
        #communicationVendorPage .filter-btn:hover {
            background: var(--primary);
            color: #fff;
        }

    /* small inputs */
    #communicationVendorPage .table-toolbar .form-control-sm {
        height: 36px;
        border-radius: 999px;
        border-color: var(--primary);
    }

    /* Export button (rounded) */
    #communicationVendorPage .export-btn {
        border-radius: 999px;
        color: #fff;
        background: var(--primary);
        padding: 6px 14px;
        font-weight: 400;
        font-size: 0.85rem;
        -webkit-tap-highlight-color: transparent;
    }

        #communicationVendorPage .export-btn:hover {
            background: var(--primary-dark);
        }

        /* remove default dark flash on click for export and other action buttons while keeping subtle ring on focus */
        #communicationVendorPage .export-btn:focus,
        #communicationVendorPage .export-btn:active {
            outline: none;
            box-shadow: 0 0 0 3px rgba(228,93,39,0.12);
        }

    /*Fix Search bar and button in toolbar*/
    #communicationVendorPage .table-toolbar .input-group .form-control {
        border-top-right-radius: 0;
        border-bottom-right-radius: 0;
    }

    #communicationVendorPage .table-toolbar .input-group .btn {
        border-top-left-radius: 0;
        border-bottom-left-radius: 0;
        border-color: var(--primary);
    }

/* Pagination area */
#communicationVendorPage .table-pagination {
    border-top: 1px solid rgba(0,0,0,0.04);
    padding-top: 8px;
    margin-top: 8px;
}

    #communicationVendorPage .table-pagination .btn {
        border-radius: 999px;
        font-size: 0.85rem;
        padding: 6px 12px;
        border: 1px solid var(--primary);
        background: #fff;
        color: var(--primary);
    }

        #communicationVendorPage .table-pagination .btn:hover {
            background: var(--primary);
            color: #fff;
            border-color: var(--primary);
        }

    #communicationVendorPage #viewAllRows {
        background-color: var(--primary);
        color: #fff;
        border: none;
    }

    #communicationVendorPage #viewAllRows:hover {
        background-color: var(--primary-dark);
    }


    /* statuses */
    #communicationVendorPage .badge {
        padding: 6px 10px;
        border-radius: 999px;
        font-size: 0.75rem;
    }

    #communicationVendorPage .status-active {
        background: #dff6e9;
        color: #197a3a;
    }

    #communicationVendorPage .status-upcoming {
        background: #ffeedf;
        color: #d06a1e;
    }

    #communicationVendorPage .status-disabled {
        background: #ffe9ee;
        color: #c43a5b;
    }

    /* Responsive tweaks */
    @@media (max-width: 767px) {
        #communicationVendorPage .table-toolbar {
            flex-direction: column;
            gap: 8px;
            align-items: stretch;
        }

        #communicationVendorPage .top-bar {
            flex-direction: column;
            gap: 8px;
            align-items: flex-start;
        }

        #communicationVendorPage .limit-instruction {
            font-size: 0.85rem;
        }

        #communicationVendorPage .toggle-pill .btn-pill {
            padding: 8px 12px;
            min-width: 64px;
        }
    }

    /* Table Buttons*/

    .btn-upload, .btn-view, .btn-submit {
        border-radius: 999px;
        border: 1px solid var(--primary);
        color: var(--primary);
        background: transparent;
        padding: 4px 8px;
        font-weight: 400;
        font-size: 0.78rem;
    }

        .btn-upload:hover, .btn-view:hover, .btn-submit:hover {
            background: var(--primary);
            color: #fff;
        }

/* Communication Vendor Modal*/

#communicationVendorPage .cv-header {
    background: var(--primary);
    color: #fff;
    padding: 12px 16px;
    border-bottom: none;
}

#communicationVendorPage .cv-header h6 {
    font-size: 0.95rem;
    font-weight: 500;
}

#communicationVendorPage .cv-close {
    filter: invert(1);
    opacity: 0.9;
}

#communicationVendorPage .cv-close:hover {
    opacity: 1;
}

#communicationVendorPage .cv-body {
    background: var(--card-bg);
    padding: 16px;
}

#communicationVendorPage .cv-body .form-label {
    font-size: 0.75rem;
    font-weight: 500;
    margin-bottom: 4px;
}

#communicationVendorPage .cv-body .form-select {
    height: 34px;
    font-size: 0.8rem;
    border-radius: 6px;
    border-color: #dcdcdc;
}

#communicationVendorPage .cv-body .form-select:focus {
    border-color: var(--primary);
    box-shadow: none;
}

#communicationVendorPage .cv-footer {
    background: var(--card-bg);
    border-top: none;
    justify-content: flex-end;
    padding: 12px 16px;
}

#communicationVendorPage .cv-btn-outline {
    border-radius: 999px;
    border: 1px solid var(--primary);
    background: transparent;
    color: var(--primary);
    padding: 4px 6px;
    font-size: 0.8rem;
}

#communicationVendorPage .cv-btn-outline:hover {
    background: var(--primary);
    color: #fff;
}

#communicationVendorPage .cv-btn-primary {
    border-radius: 999px;
    background: var(--primary);
    color: #fff;
    border: none;
    padding: 4px 6px;
    font-size: 0.8rem;
}

#communicationVendorPage .cv-btn-primary:hover {
    background: var(--primary-dark);
}


</style>


<div id="communicationVendorPage" class="px-4 py-2">

    <!-- Top bar: Heading left + Create Compliance Document button right -->
    <div class="d-flex align-items-center justify-content-between mb-3 top-bar">
        <h4 style="color: #E45D27">Communication Page</h4>

        <div class="d-flex align-items-center gap-2">
            <!-- Create Compliance Document (rounded pill) -->
            <button class="btn btn-sm " id="createComplDoc" type="button" data-bs-toggle="modal" data-bs-target="#createComplianceDocModal">
                <i class="bi bi-plus-lg" aria-hidden="true"></i>
                <span>Create Compliance Doc</span>
            </button>
        </div>
    </div>

    <!-- Create Compliance Doc Modal -->
    <div class="modal fade" id="createComplianceDocModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-md modal-dialog-centered">
            <div class="modal-content">

                <!-- Header -->
                <div class="modal-header cv-header">
                    <h6 class="mb-0">Create Compliance Document</h6>

                    <!-- CLOSE BUTTON -->
                    <button type="button" class="btn-close" data-bs-dismiss="modal" style="color:#fff"></button>
                </div>

                <!-- --Body-- -->
                <div class="modal-body cv-body">
                    <div class="row g-3">

                        <div class="col-md-6" ng-init="AllParty()">
                            <label class="form-label">Client</label> 
                            <select id="ddlClient" autocomplete='address-level2' class="form-select form-select-sm clear validate" ng-model="ClientId" ng-change="BindStatusInvoice()" ng-disabled="freeze">
                                <option value="">Select</option>
                                <option ng-repeat="x in AllPartyList | filter: { PartyType: 'Client' }" value="{{x.PartyId}}">{{x.PartyName}}</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">State</label>
                             
                            <select id="ddlstate" autocomplete='address-level2' class="form-select form-select-sm clear validate" ng-disabled="freeze" ng-init="AllState()" ng-model="StateId" ng-change="AllPartySiteLoadWI();WIBindMonth();WIBindFinacialYear();">
                                <option value="">Select State</option>
                                <option ng-repeat="x in AllStateList" value="{{x.SATE_CODE}}">{{x.STATE_NM}}</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Financial Year</label>
                          
                            <select id="ddlfyid" autocomplete='address-level2' class="form-select form-select-sm clear validate" ng-model="FyId" ng-change="BindIn(FyId)" ng-disabled="freeze">
                                <option value="">Select</option>
                                <option ng-repeat="x in WIfinacialyearList" value="{{x.FinancialName}}">{{x.FinancialName}}</option>
                            </select>

                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Month</label>
                           
                            <select id="ddlMONTH" autocomplete='address-level2' class="form-select form-select-sm clear validate" ng-model="Month" ng-change="ChangeMonth()" ng-disabled="freeze">
                                <option value="">Select</option>
                                <option ng-repeat="x in WIMonthList" value="{{x.MonthNumber}}">{{x.MonthName}}</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Site</label>
                            
                            <select class="form-select form-select-sm clear  validate" ng-model="WClientSiteId" ng-change="SetSingleLocation(WClientSiteId)">
                                <option value="">Select Site</option>
                                <option ng-repeat="x in AllPartySiteVList" value="{{x.SiteId}}">{{x.SiteName}}</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label class="form-label">Location</label>
                         
                            <select id="ddlLocation" autocomplete='address-level2' class="form-select form-select-sm  clear validate" ng-model="WLocation">
                                <option value="">Select Location</option>
                                <option ng-repeat="x in LocationL" value="{{x.Location}}">{{x.Location}}</option>
                            </select>
                        </div>

                    </div>
                </div>

                <!-- Footer -->
                <div class="modal-footer cv-footer">
                    <button class="btn btn-sm cv-btn-outline" data-bs-dismiss="modal"  ng-model="ClearH" ng-click="ClearReset()" >
                        Cancel
                    </button>
                    <button class="btn btn-sm cv-btn-primary" ng-click="ValidateWithoutInvoice()" >
                        Create
                    </button>
                </div>

            </div>
        </div>
    </div>



    <div class="card section-card">
        <div class="card-body p-3 h-100 d-flex flex-column">

            <!-- Toolbar -->
            <div class="table-toolbar d-flex align-items-center justify-content-between mb-2 gap-2 flex-wrap">
                <div class="d-flex align-items-center gap-2">

                    <!-- STATUS -->
                    <div class="dropdown">
                        <button class="btn btn-sm filter-btn dropdown-toggle" data-bs-toggle="dropdown">
                            {{selectedStatus || 'Status'}}
                        </button>
                        <ul class="dropdown-menu">
                            <li ng-repeat="s in StatusList">
                                <a class="dropdown-item" href="" ng-click="setFilter('status', s)">
                                    {{s}}
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- STATE -->
                    <div class="dropdown">
                        <button class="btn btn-sm filter-btn dropdown-toggle" data-bs-toggle="dropdown">
                            {{selectedState || 'State'}}
                        </button>
                        <ul class="dropdown-menu">
                            <li ng-repeat="s in StateList">
                                <a class="dropdown-item" href="" ng-click="setFilter('state', s)">
                                    {{s}}
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- MONTH -->
                    <div class="dropdown">
                        <button class="btn btn-sm filter-btn dropdown-toggle" data-bs-toggle="dropdown">
                            {{selectedMonth || 'Month'}}
                        </button>
                        <ul class="dropdown-menu">
                            <li ng-repeat="m in MonthList">
                                <a class="dropdown-item" href="" ng-click="setFilter('month', m)">
                                    {{m}}
                                </a>
                            </li>
                        </ul>
                    </div>

                    <!-- YEAR -->
                    <div class="dropdown">
                        <button class="btn btn-sm filter-btn dropdown-toggle" data-bs-toggle="dropdown">
                            {{selectedYear || 'Year'}}
                        </button>
                        <ul class="dropdown-menu">
                            <li ng-repeat="y in YearList">
                                <a class="dropdown-item" href="" ng-click="setFilter('year', y)">
                                    {{y}}
                                </a>
                            </li>
                        </ul>
                    </div>

                    <button class="btn btn-sm filter-btn active" ng-click="applyFilter()">Go</button>
                    <button class="btn btn-sm btn-secondary" ng-click="resetFilter()">Reset</button>

                </div>

                <div class="d-flex align-items-center gap-2 flex-grow-1 justify-content-center justify-content-md-end">
                    <div class="flex-grow-1 me-2 search-container" style="max-width:360px;">
                        <div class="input-group input-group-sm">
                            <input id="scSearch"
                                   type="text"
                                   ng-model="Searchbox"
                                   class="form-control form-control-sm"
                                   placeholder="Search..." />
                            <button class="btn btn-outline-secondary" ng-click="Searchbox=''">✖</button>
                            <button class="btn btn-sm" id="scSearchBtn" style="border-color: #E45D27" type="button"><i class="bi bi-search"></i></button>
                        </div>
                    </div>

                    <div>
                        <div class="dropdown">
                            <button class="btn btn-sm export-btn dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">Export</button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="" ng-click="exportCSV()">CSV</a></li>
                                <li><a class="dropdown-item" href="" ng-click="printData()">Print</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Table wrapper -->
            <div class="small-table-wrapper flex-grow-1 overflow-auto rounded-3" ng-init="BindCommunication()">
                <div class="table-responsive">
                    <table class="table table-sm mb-0 small-table">
                        <thead>
                            <tr>
                                <th>S.No</th>
                                <th>Client</th>
                                <th>State</th>
                                <th>Financial Year</th>
                                <th>Month</th>
                                <th>Site</th>
                                <th>Location</th>
                                <th>Status</th>
                                <th>Unique No.</th>
                                <th>Creation Date</th>
                                <th>Submission Updated</th>
                                <th>Days Left</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody >
                            <tr ng-repeat="x in FilteredList | filter:Searchbox track by $index">
                                <td>{{$index+1}}</td>
                                <td>{{x.PartyName}}</td>
                                <td>{{x.StateName}}</td>
                                <td>{{x.FYID}}</td>
                                <td>{{x.InMonthName}}</td>
                                <td>{{x.SiteName}}</td>
                                <td>{{x.Location}}</td>
                                <td>{{x.Status}}</td>
                                <td>{{x.InvoiceNo}}</td>
                                <td>{{x.CreatedOn}}</td>
                                <td>{{x.UpdatedON}}</td>
                                <td>{{x.DayLeft}}</td>

                                <td>
                                    <button class="btn btn-sm btn-submit px-3" data-bs-toggle="modal" data-bs-target="#submitComplianceModal" ng-click="GetDocument(x.ConversationId)">Click here to Upload Doc</button>
                                </td>
                            </tr>
 
                        </tbody>

                    </table>
                </div>
            </div>
        </div>


        <!-- ================= Submit Modal ================= -->
        <div class="modal fade"
             id="submitComplianceModal"
             tabindex="-1"
             aria-hidden="true">

            <div class="modal-dialog modal-fullscreen">
                <div class="modal-content">

                    <!-- HEADER -->
                    <div class="modal-header" style="background: var(--primary); color: #fff">
                        <h5 class="modal-title">Submit Documents</h5>

                        <!-- DEFAULT BOOTSTRAP CLOSE -->
                        <button type="button" ng-click="BindCommunication()"
                                class="btn-close"
                                data-bs-dismiss="modal"
                                aria-label="Close"></button>
                    </div>

                    <!-- BODY -->
                    <div class="modal-body p-8" style="background: var(--card-bg)">
                      
                        <div class="d-flex justify-content-end mb-2">
                            <button class="btn btn-sm export-btn" ng-click="ChkDefaultFile()"> 
                                Send To Auditor
                            </button>
                            <button class="btn btn-sm export-btn"  ng-click="downloadAll()">
                                <i class="bi bi-download me-1"></i>
                                Download All
                            </button>
                        </div>

                        <!-- Table wrapper -->
                        <div class="small-table-wrapper flex-grow-1 overflow-auto rounded-3">
                            <div class="table-responsive">
                                <table class="table table-sm mb-0 small-table">
                                    <thead>
                                        <tr>
                                            <th style="width:40px;">
                                                <input type="checkbox" />
                                            </th>
                                            <th>S. No</th>
                                            <th>Document Name</th>
                                            <th>Type</th>
                                            <th>Frequency</th>
                                            <th>Mandatory</th>
                                            <th>Required Format</th>
                                            <th>Note</th>
                                            <th>Upload</th>
                                            <th>View</th>
                                            <th>Action</th>
                                            <th>Status</th>
                                        </tr>
                                    </thead>
                                    <tbody>

                                        <tr ng-repeat="xx in InDocList">
                                            <td>
                                                <input type="checkbox" ng-checked="{{xx.IsDefault}}" />
                                            </td>
                                            <td>{{xx.SRNO}} </td>
                                            <td>{{xx.DocumentName}}</td>
                                            <td>{{xx.DocumentType}}</td>
                                            <td>{{xx.Frequency}}</td>
                                            <td>{{xx.Mandatory}}</td>
                                            <td>{{xx.FormatType}}</td>
                                            <td>{{xx.Note}}</td>
                                         
                                            <td>

                                                <div class="d-flex align-items-center" ng-if="xx.Status!=1">
                                                    <label class="btn btn-sm btn-upload px-3"
                                                           style="white-space:nowrap; cursor:pointer;"
                                                           ng-class="{'disabled': TimeLeftMsg}">

                                                        Choose File

                                                        <input type="file"
                                                               id="fuCandidatePhoto{{$index}}"
                                                               style="display:none"
                                                               ng-disabled="TimeLeftMsg"
                                                               onchange="angular.element(this).scope().show(this, '#AlbumIcon')"
                                                               ng-model="InvoiceDetail[$index].fileupload">
                                                    </label> 
                                                </div>
                                            </td>
                                            <td>
                                                <button ng-if="xx.DocumentFile" class="btn btn-sm"
                                                        ng-click="ViewFileC(xx.DocumentFile)">
                                                    <i class="bi bi-eye"></i>
                                                </button>

                                                <button ng-if="xx.DocumentFile" class="btn btn-sm"
                                                        ng-click="DownloadFileC(xx.DocumentFile, xx.DocumentName)">
                                                    <i class="bi bi-download"></i>
                                                </button>
                                            </td>
                                            <td>

                                                <div ng-hide="{{TimeLeftMsg}}" ng-if="xx.Status==0" class="btn btn-sm cv-btn-primary" ng-click="ValidateFileDoc(xx.RegInvoiceNo,xx.SRNO,xx.DocumentName,xx.DocumentId,xx.FormatType,$index,xx.ConversationId)">Upload</div>
                                                <div ng-if="xx.Status==2" class="btn btn-sm cv-btn-primary" ng-click="ValidateFileDoc(xx.RegInvoiceNo,xx.SRNO,xx.DocumentName,xx.DocumentId,xx.FormatType,$index,xx.ConversationId)">Re-Upload</div>
                                                <div ng-if="xx.Status==3" class="btn btn-sm cv-btn-primary" ng-click="ValidateFileDoc(xx.RegInvoiceNo,xx.SRNO,xx.DocumentName,xx.DocumentId,xx.FormatType,$index,xx.ConversationId)">New Req To Upload</div>

                                            </td>
                                            <td>
                                                {{ xx.Status === 1 ? 'Complete' :     (xx.Status === 0 ? 'Pending' :       (xx.Status == null || xx.Status === '' ? 'Draft' : 'Draft')) }}
                                            </td>
                                            @*<td><span class="badge status-active">Validated</span></td>*@
                                        </tr>



                                    </tbody>

                                </table>




                            </div>
                        </div>

                    </div>

                    <!-- FOOTER -->
                    <div class="modal-footer justify-content-end" style="background: var(--card-bg)">
                        <button class="btn btn-sm cv-btn-outline" id="btnCancel" ng-click="BindCommunication()"
                                data-bs-dismiss="modal">
                            Cancel
                        </button>

                        @*<button class="btn btn-sm cv-btn-outline">
                            Save Draft
                        </button>

                        <button class="btn btn-sm cv-btn-primary">
                            Submit
                        </button>*@
                    </div>

                </div>
            </div>
        </div>


    </div>
</div>

